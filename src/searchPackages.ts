// Search for a package using a regular expression over package names and READMEs. 
// The results of a package search should resemble the “directory” view, but a subset thereof

//iterate through each package
//search the package name
//search the READMEs

//create a directory of the packages that match the search